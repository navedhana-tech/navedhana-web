# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment. Values here are VITE_* (inlined into the client
# bundle and therefore public), but keeping the file untracked stops a
# genuinely secret value from being committed later by habit.
.env
.env.local
.env.*.local
